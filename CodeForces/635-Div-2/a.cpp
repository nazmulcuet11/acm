#include <iostream>

using namespace std;
typedef long long ll;

int main(int argc, char const *argv[])
{
    freopen("input.txt", "r", stdin);
    int test_case;
    cin >> test_case;
    while (test_case--)
    {
        ll a, b, c, d;
        cin >> a >> b >> c >> d;
        cout << b << " " << c << " " << c << endl; 
    }
    
    return 0;
}
